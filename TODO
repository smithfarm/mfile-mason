# Short-term
20131010 At login, put user id of logged-in user into Global hash
20131009 Add database stuff to MAC address generator:
         - insert a dummy record
	 - fetch the dummy record to get its id
	 - update the record with MAC address, username, timestamp,
	   etc.
20131009 Allow "admin users" to log in without checking password
20131009 "Mouse users click here" button to display help text about
how the cursor must be visible in input field, press TAB, etc.

# Long-term
20131007 Package in OBS
20131009 Fix Apache configuration to prevent user from calling a
component directly (change privs so subdirectories cannot be accessed
remotely?)
20131009 Implement "Hardware for decommissioning" app
20131010 Come up with some way of dealing with DBI errors (raise an
exception?)
20131010 Add password field to users table (for use when LDAP is
disabled) -- for now, passwords to be set manually via mysql client

